#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
	    int n;
	    cin >> n;
	    vector<int> a(n);
	    vector<int> b(n);
	    for(int i=0;i<n;i++){
	        cin >> a[i];
	    }
	    for(int i=0;i<n;i++){
	        cin >> b[i];
	    }
	    if(a==b){
	        cout << "yes" << endl;
	    }
	   // else if(n<3){
	   //     int f =0;
	   //     for (int i = n - 1; i > 0; i--) {
    //     if ((a[i-1] < a[i] && b[i-1] > b[i]) ||(a[i-1] > a[i] && b[i-1] < b[i]))
    //     {
    //         f = 1;      
    //         break;
    //     }
    // }
	        
	        
	   //     if(f){
	   //         cout << "no" <<endl;
	   //     }
	   //     else{
	   //         cout << "yes" << endl;
	   //     }
	        
	   // }
	   // else{
	   //     cout <<  "yes" << endl; 
	   // }
	   else{
	       int f=0;
	       for(int i = 0; i  < n-1; i++) {
            if((a[i] < a[i+1]) != (b[i] < b[i+1])) 
            {
                f = 1;
                break;
            }}
              if(f){
	           cout << "no" << endl;
	       }
	       else{
	           cout << "yes" << endl;
	       }
        
        }
	     
	   }
	}
