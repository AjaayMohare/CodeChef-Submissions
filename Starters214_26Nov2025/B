#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
	    int n;
	    cin >> n;
	    vector<int> v(n);
	   // for(int i=0;i<n;i++){
	   //     cin >> v[i];
	   // }
	  
	       int ans = 0;
         
	       for(int i=1;i<n;i++){
	           if(i%2==0){
	               ans+=(n-i-1);
	           }
	       }
	       cout << ans << endl;
	   
	  
	    
	}

}
