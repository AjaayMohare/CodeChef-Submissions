#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
	    int n;
	    cin >> n;
	    string s;
	    cin >> s;
	    int gv=0;
	    int ng=0;
	    int ways=0;
	    for(int i=0;i<n;i++){
	        if(s[i]=='1') gv++;
	        else ng++;
	        if(gv > ng) ways++;
	    }
	    cout <<ways << endl;
	}
}
