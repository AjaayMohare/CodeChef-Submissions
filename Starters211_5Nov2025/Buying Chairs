#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
	    long long w,p,k;
	    cin >> w >> p >> k;
	    if(w>=k){
	        cout << k*2 << endl;
	    }
	    else{
	        cout << w*2 + (k-w) << endl;
	    }
	}

}

